[pytest]
asyncio_mode = auto
# 测试函数与 fixture 统一使用会话级事件循环：
# 避免连接在函数循环创建、fixture 循环关闭的跨循环 greenlet 损坏
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests